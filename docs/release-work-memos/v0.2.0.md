# Release Work Memo v0.2.0

この文書は release gate evidence の正本とする。

## Scope

- Minor release for the main-owned interactive AI change proposal lifecycle and GPT-5.6 model registry picker.
- Adds per-hunk apply / discard / manual edit with main-side canonicalization, CAS revision checks, baseline revalidation, stale rejection, and terminal indeterminate outcomes that prevent automatic retry when apply acknowledgement is unavailable.
- Adds GPT-5.6 Sol / Terra / Luna selection, Terra default, shared runtime / metadata facts, standard and long-context pricing display, and explicit legacy migration.
- Keeps dynamic model discovery, automatic price sync, usage metering, reasoning controls, provider catalogs, and broader proposal targets out of this release scope.

## Browser Gate

- command: `npm run preview -- --host 127.0.0.1 --port 4173 --strictPort` plus `npx playwright test --config=playwright.manual-server.config.ts --workers=1` using a temporary config with Playwright's built-in `webServer` disabled.
- saved file result: pass as part of the 71-test browser suite.
- preview result: pass as part of the 71-test browser suite.
- draft workspace result: pass as part of the 71-test browser suite.
- evidence path or screenshot note: terminal result `71 passed (2.0m)` on 2026-07-13 JST; the temporary config was removed after the run.
- notes: the normal Playwright `webServer` probe hung against the no-listener localhost port in this WSL environment. An initial 11-worker run had one transient status-observation timeout (`70 passed, 1 failed`); the targeted test then passed and the complete serial rerun passed. The suite includes the Settings model picker and interactive proposal regressions.

## Build Gate

- command: `npm run lint`; `npm run build`
- result: pass; pass.
- evidence path or screenshot note: terminal output on 2026-07-13 JST.
- notes: the production build retained the existing Vite chunk-size warning; no build error occurred.

## Electron Regression Gate

- command: `npm run test:e2e:electron`
- result: pass, 49 tests.
- evidence path or screenshot note: terminal result `49 passed (1.8m)` on 2026-07-13 JST.
- notes: the first run exposed a stale 30,000-character large-preview fixture after the GPT-5.6 context budget increased the inline-preview cap. Commit `ec2fdeb` updates the fixture to 250,000 characters; the targeted regression and full suite then passed. Coverage includes proposal request / IPC / apply lifecycle and the GPT-5.6 runtime reasoning request shape.

## Node Regression Gate

- command: `npm run test:node`
- result: pass, 97 tests.
- evidence path or screenshot note: terminal output on 2026-07-13 JST.
- notes: covers registry facts, model update validation, proposal lifecycle, and release helpers.

## Electron Manual Smoke

- first save continuity: not applicable for this release.
- HTML export continuity: not applicable for this release.
- broken image fallback: not applicable for this release.
- unresolved image visibility: not applicable for this release.
- evidence path or screenshot note: no image continuity or fallback behavior changes in this release.
- export or workspace path: not applicable.
- failure-free result note: full Electron regression passed, 49 tests.
- notes: proposal behavior is covered by dedicated Electron E2E rather than an image smoke.

## Packaged Candidate Spot Check

- candidate launch path: `C:\Users\katsumata-m\AppData\Local\MarkDownViewer\latest\MarkDownViewer.exe`, deployed from `release/windows-host-candidate/win-unpacked`.
- first save continuity: not applicable for this release.
- HTML export continuity: not applicable for this release.
- broken image fallback: not applicable for this release.
- unresolved image visibility: not applicable for this release.
- evidence path or screenshot note: `npm run release:check:candidate` passed; the deployed application was launched with Chromium remote debugging enabled and its packaged `settings.html` and preload-backed metadata were inspected through the DevTools protocol.
- failure-free result note: candidate generation, integrity check, local deployment, packaged launch, and promotion all passed.
- notes: generated with `npm run win:host:generate:clean:noadmin`, deployed with `npm run win:host:deploy:candidate:noadmin`, and promoted with `npm run win:host:promote:noadmin` on 2026-07-13 JST.

## Model Registry Preflight

- applicable: yes
- result: pass against the deployed Windows candidate.
- notes: packaged Settings exposed exactly three selectable candidates: Sol (`$5.00 / $30.00`), Terra (`$2.50 / $15.00`, recommended), and Luna (`$1.00 / $6.00`), all with a 1,050,000-token context. Existing `gpt-5.4-mini` remained as a disabled legacy option with an explicit migration warning. Selecting Terra without saving showed cached input `$0.25`, 128,000 maximum output, and the above-272,000-token `2x` input / `1.5x` output condition. Packaged `get_app_metadata` returned version `0.2.0`, tag `v0.2.0`, platform `win32`, default `gpt-5.6-terra`, registry version `2026-07-13`, the same three model facts, and `selectedModelKnown: false` for the legacy value.

## Release Workflow Validation

- command: `npm run test:release`; `npm run release:check:candidate`; `npm run release:check -- --skip-git`
- result: pass, 12 release tests; candidate integrity pass; promoted artifact integrity pass.
- evidence path or screenshot note: `release/windows-host/artifact-metadata.json` records generatedAt `2026-07-13T10:08:29.9140901+00:00`; `release/windows-host/installer/latest.yml` records v0.2.0 hashes and sizes.
- notes: the clean-worktree form of `npm run release:check` is run after the release commit, before push and tag publication.

## Follow-ups

- blocker or residual risk: automated and candidate validation gates pass. Exact-diff review must be rerun on this amended staged diff before the release commit; clean-worktree validation, push, tag, and GitHub publication then remain.
