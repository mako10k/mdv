# Release Work Memo v0.1.14

この文書は release gate evidence の正本とする。

## Scope

- Relative image handling bundle for MD-BL-005 / MD-BL-023.
- Help surface addition and footer guidance cleanup included in the shipped UI slice.

## Browser Gate

- command: `npm run build && npx playwright test tests/e2e/app-layout.spec.ts -g "WYSIWYG resolves saved relative images to actual image sources|preview resolves saved relative images to actual image sources|WYSIWYG resolves draft-workspace relative images for unsaved documents"`
- saved file result: pass
- preview result: pass
- draft workspace result: pass
- evidence path or screenshot note: command re-run under `mdv@0.1.14` on 2026-06-10 and finished with 3 passed.
- notes: browser gate confirms the renderer-side saved-file preview path and WYSIWYG resolution path for both saved and draft-relative images.

## Electron Manual Smoke

- first save continuity: pass via `npm run test:e2e:electron -- --grep "repeated pasted images|repeated dropped images|saved relative image export"` on 2026-06-12; this includes the repeated paste first-save regression added after packaged smoke found adjacent inline data-image leakage.
- HTML export continuity: pass via `npm run test:e2e:electron -- --grep "repeated pasted images|repeated dropped images|saved relative image export"` on 2026-06-12; the new export regression opens a saved relative image document, exports HTML, and verifies the exported HTML contains the expected title, `data:image/svg+xml;base64,`, and image alt text.
- broken image fallback: proxy pass via `npm run test:e2e:electron -- --grep "missing relative image shows a preview fallback"` on 2026-06-10 after the follow-up fix.
- unresolved image visibility: proxy pass via the packaged candidate unresolved image visibility check below; ADR 0020 removes asset materialization as a release criterion, so this gate now records whether unresolved image state is visible instead of silent.
- evidence path or screenshot note: terminal output showed 3 passed on 2026-06-12 for the targeted repeated paste, repeated drop, and saved relative image export gate.
- export or workspace path: saved relative image export is covered by `tests/e2e-electron/autosave-recovery.spec.ts`; unresolved image visibility evidence is recorded in `release/packaged-smoke-missing-preview.png`.
- failure-free result note: targeted Electron E2E passed without failures.
- notes: ADR 0020 changed the pass criterion here from `assets/` folder generation to whether the image remains visible and editable after first save.

## Follow-up Gate

- inline data-image preview: pass via `npm run test:e2e -- --grep "preview renders inline data image markdown as an image"` on 2026-06-10.
- repeated image Drag&Drop: pass via `npm run test:e2e:electron -- --grep "dropped image|repeated dropped"` on 2026-06-10.
- saved preview continuity: pass via `npm run test:e2e:electron -- --grep "pasted image remains visible in preview after first save"` on 2026-06-10.
- missing-image fallback: pass via `npm run test:e2e:electron -- --grep "missing relative image shows a preview fallback"` on 2026-06-10.
- repeated pasted image first-save: pass via `npm run test:e2e:electron -- --grep "repeated pasted images|repeated dropped images|saved relative image export"` on 2026-06-12.
- notes: The repeated Drag&Drop and repeated paste regressions dispatch two image files into the editor, assert source view abbreviates inline image data without raw base64 or raw SVG leakage, and verify the saved Markdown keeps both inline `data:image...` sources as the canonical representation.

## Packaged Candidate Spot Check

- deployed candidate path: `C:\Users\勝又誠\AppData\Local\MarkDownViewer\latest\MarkDownViewer.exe`
- first save continuity: packaged manual pass on 2026-06-12; Windows-local clipboard paste from `C:\Users\勝又誠\AppData\Local\Temp\mdv-manual-smoke-clipboard.png` stayed usable after save and WYSIWYG round-trip without duplicate raw image payload insertion.
- HTML export continuity: packaged pass on 2026-06-12; `release/packaged-smoke-saved-preview-export.html` was written from the deployed candidate and contains `Saved Preview Image`, `data:image/svg+xml;base64,`, and `alt="preview-diagram"`.
- broken image fallback: packaged visual pass on 2026-06-12; `release/packaged-smoke-missing-preview.png` shows `Missing image: assets/missing-diagram.svg`.
- saved relative image preview: packaged visual pass on 2026-06-12; `release/packaged-smoke-saved-preview.png` shows the saved relative SVG rendered in preview.
- unresolved image visibility: packaged visual pass on 2026-06-12 for the ADR 0020 state-visibility criterion; unresolved image state is visible instead of silent through the `Missing image: assets/missing-diagram.svg` fallback in the deployed candidate.
- evidence path or screenshot note: `npm run win:host:generate:clean:noadmin`, `npm run release:check:candidate`, and `npm run win:host:deploy:candidate:noadmin` all passed on 2026-06-12 JST after the paste propagation fix. Candidate-side metadata at `release/windows-host-candidate/artifact-metadata.json` recorded `version: 0.1.14`, `releaseTag: v0.1.14`, and `generatedAt: 2026-06-12T05:38:45.8815114+00:00`.
- failure-free result note: packaging, candidate integrity check, candidate deployment, packaged first-save clipboard paste, packaged saved relative image preview, packaged saved relative image HTML export, packaged missing fallback, and unresolved image visibility completed without failures.
- notes: the old packaged candidate from 2026-06-10 is invalidated and replaced by the 2026-06-12 JST candidate. `assets/` directory generation is no longer a release success criterion after ADR 0020.

## Model Registry Preflight

- applicable: no
- result: not required for this release line
- notes: no model-registry-specific release change in this slice

## Follow-ups

- blocker or residual risk: publishing still requires the normal release check/push/tag/GitHub release steps.
