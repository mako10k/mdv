# MDV 0.1.15

## Highlights

- Restores rendered preview output for code fences that fall back to Markdown-it rendering, such as `c++` fences.

## Fixes

- Fixes fallback code blocks appearing with zero height or disappearing in rendered preview.
- Keeps fallback code block styling aligned in rendered preview, AI chat Markdown, and exported HTML.
- Fixes WYSIWYG footnote insertion so the selected text is used as the footnote definition and the marker is inserted at the selected anchor.

## Notes

- Adds Playwright coverage for fallback code block height and WYSIWYG footnote insertion.
